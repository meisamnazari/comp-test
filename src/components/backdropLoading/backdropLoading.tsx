import React, { FC } from "react";
import { BackdropLoadingProps } from "./types";

const CustomBackdropLoading: FC<BackdropLoadingProps> = ({
  onClose,
  open = true,
}) => {
  return <div>Back dropppppppppppp</div>;
};

export default CustomBackdropLoading;
